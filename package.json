{
  "name": "tienda-multimarca",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "preview": "astro build && wrangler dev",
    "cf-typegen": "wrangler types",
    "deploy:sanacion": "astro build && wrangler deploy --env sanacion",
    "deploy:guias": "astro build && wrangler deploy --env guias",
    "pdfs:local": "node scripts/upload-pdfs.mjs --local",
    "pdfs:remote": "node scripts/upload-pdfs.mjs --remote"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^24.13.4",
    "typescript": "^5.9.3",
    "wrangler": "^4.131.2"
  }
}
